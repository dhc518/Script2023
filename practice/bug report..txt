(1) 문제점을 어떤 식으로 찾았는지에 대한 설명:
 랜덤 함수를 사용한 subcount를 랜덤 범위에 포함되는 고정된 정수로 반복해서 실행해봤다.

(2) 문제점이 어떤 것인지에 대한 설명:
random.randint(0, count)에서 count가 0일 경우 함수가 종료되지 않고, 
계속해서 하위 폴더를 생성하려고 하면서 무한 루프에 빠지는 문제점이 있다. 
이로 인해, 폴더 생성이 끝나지 않고 프로그램이 멈추는 상황이 발생합니다.

(3) 어떤 식으로 해결했는지에 대한 설명:
count가 0일 경우 함수를 빠져나오도록 
if not count: return 을 if count <= 0: return 로 수정하였다.

추가적으로 재귀함수를 실행하고 나서, 디렉터리를 상위폴더로 바꾸지 않고 생성한 폴더로 계속 내려 가기 때문에 랜덤한 폴더 구조가 생기지 않아서, 만약 재귀함수의 호출이 종료되었다면 상위 폴더로 돌아가는 
os.chdir('..') 코드를 make_random_folders() 마지막에 작성하였다.