import subprocess
import sys
# subprocess.run('c:/Windows/system32/calc.exe')
#
# subprocess.run(['c:/Windows/notepad.exe', 'simple.txt'])
#
# subprocess.run([sys.executable, 'hello.py'])

subprocess.run(['start', '\lesson\TimeThread\kitty.jpg'], shell=True)
